# 継続メモ — 2026-08-17

## ▶ 次のセッションの起点（1行）

**`scripts/chiba-hold-and-next-2026-08-17.md` §2 を Manus に投げる。①きさらづの住所・電話 → ②南房総市の HIGH 8件 → ③富士河口湖町の L1 代替、の順。**

---

## 1. 今日やったこと

### 案C — 完了

| | 基準線(76地区) | 案C後(18地区) | 判定 |
|---|---:|---:|---|
| 地区数 | 76 | **18** | ✅ |
| **包含ペア** | 7 | **0** | ✅ |
| どの地区にも入らない項目（126件） | 106※ | **0** | ✅ |
| **MISSING ユニーク** | 184 | **249** | ✅（減っていない） |
| MISSING 延べ | 214 | 268 | |
| IN_DATA / ORPHAN（延べ） | 103 / 48 | 92 / 43 | 下記 |
| 牧野の必須検証 | PASS | **PASS**（`相模原市` で） | ✅ |
| 大字検査 | — | **5件**（不一致3 / 包含2） | ✅ |

※ 基準線に焼いてある126件は japancamp が生きていた頃の実測値。同じ定義でいま数えると106件。

**`--all` は `needsVerifyDistricts()` を通っていた**（§20-11）。76地区ではなく**33地区**しか
回っておらず、**穴の実サイズは275件**だった。「126件」はその絞り込みを含んだ数字。

### 下振れ2つは説明済み — **どちらも robots ガードの代償で、案Cの成績ではない**

| | 試算 | 実測 | 理由 |
|---|---:|---:|---|
| 新しい MISSING | +93 | **+65** | 新規 +87 / 消失 −22。**消失の14件が japancamp と富士河口湖町観光連盟の喪失**。8件は誤検出・重複の解消（3件は MISSING→IN_DATA）。**検出力は落ちていない** |
| b2 | 1,352前後 | **366** | ①地区統合(76→18・1市町村あたり平均3.8地区) ②同一市内の大字外が定義上0 ③**japancamp が0件**（旧 b2 の 35,810件を単独で占めていた）。①②だけなら試算どおり |

**新規87件のうち82件が「旧76地区のどれにも入らなかった項目」由来。**狙いどおりの出どころ。

**IN_DATA −11 / ORPHAN −5 も全部たどれた。**延べとユニークの差（10と4）は包含7組が
同じレコードを2回数えていた分。実際に分類が変わったのは**3件だけ**。

詳細: `scripts/planc-results-2026-08-16.md`

### 千葉 — 接続してレコード3件を投入

- `MUNI_SOURCES` に接続。`--all` は **18 → 26**。既存18は1件も動いていない
  （`.check-chiba-connect.js`。**スイープし直さずに `sourcesFor` と md のソース表を突き合わせた**）
- 8市町スイープ: **MISSING 64（HIGH 26）**。IN_DATA 0 / ORPHAN 0 は**レコード0件による構造的なもの**
- **候補に無いのに MISSING に出たのが40件**（HIGH 8はすべて南房総市）＝本当の掲載漏れ候補
- L1 網羅率は**既存のやり方では測れなかった**（母数＝レコードが0件）。
  verify A/B を母数に置き換えて実測: 県台帳20%（公営のみの台帳なので仕様）/ 君津市公式82%

### ★ 両方の挙動を確認した — **案Cの機械が千葉のデータで動いている**

| id | 予告 | 実測 |
|---|---|---|
| `orange-mura-auto` | MISSING → **IN_DATA** | ✅ 番地一致（名前は不一致。観光協会は「千倉オレンジセンター」名義） |
| `otaki-kenminnomori` | MISSING → **IN_DATA** | ✅ 名前一致 |
| `otaki-sabo` | どのソースにも無い → **ORPHAN** | ✅ **投入前に予告を書いてから投入した**（`chiba-batch2-prediction-2026-08-17.md`） |

`otaki-sabo` の ORPHAN は**予告どおりで正常。**§6-7 のとおり ORPHAN は不在の証明ではなく、
**この2件が L1/L2 に載っていないことは投入前に実測済み**（大多喜町の L1 は公営のみの県台帳）。
**status を変える根拠には一切使わない。**

### 新しく入れた仕組み

| | 中身 | 偽ゼロ検証 |
|---|---|---|
| **`scoresVerified`** | **明示 `false` のときだけ** value 帯検査を飛ばす。「未指定＝評価済み」と推定しない（`priceVerified` を priceNote から機械的に付けた 9fd15e3 と同じ轍を避ける） | `.mock-scores-verified-test.js`（11件） |
| **千葉の bounds** | `isOutOfBounds` は**県が未知だと false を返す**ので、足さないと**検査が沈黙する** | `.mock-prefecture-bounds-test.js`（23件） |
| **県チップをデータで決める** | `active` が1件でもある県だけ出す。千葉は全部 `unverified` なので出ない。**active になれば自動で出る** | `.mock-filterbar-prefectures-test.js`（10件） |
| **凍結をハッシュで持つ** | 案Cの `--all` が凍結76本のうち3本を**同名で上書きした**（§20-14）。基準を md からスナップショットに移した | `.mock-baseline-freeze-test.js`（15件） |
| **md の内訳表をバックティックで** | 集計側の正規表現が `\| MISSING \| 52 \|` を拾って冒頭サマリを上書きしていた | `.mock-md-summary-parse-test.js`（9件） |

### 「内」バグの実データ検証

`大多喜SABO` の住所 **夷隅郡大多喜町堀之内595** で確認。

```
旧（バグ時の記録）  夷隅郡大多喜町堀之     ← 「内」が削れていた
いま               夷隅郡大多喜町堀之内   ✅  oaza=堀之内 / banchiKey=堀之内595
```

---

## 2. 未解決（持ち越し）

| # | 中身 | メモ |
|---|---|---|
| 1 | **`namesMatch` の緩さ**（§20-13） | `ペンギン村` と `朝霧高原英知の杜` を結んでいるのは `朝霧高原` の1語。**§19-6(a) と同じ橋渡しで、案Cは被害者を変えただけ。**`classify` が `records` の並び順で勝者を決めているのも弱い。**直し方は測ってから**（「短い名前を弾く」は否定済み） |
| 2 | **きさらづCAMP の住所・電話** | precheck 本文は「一次情報に無い → 未確認」なのに roster の住所欄に入っていた（§20-15）。**DMO の URL も記録が無い** |
| 3 | 146 → 136 の10件 | 2実装が136で一致しているので数え方ではなくソース側の変化。**当時の明細を残していないので特定できない** |
| 4 | 座標 | 千葉3件とも `lat:0 / lng:0 / needsCoord`。**しゅんの手作業（地図ピン）待ち** |
| 5 | 電話の実測 | `otaki-kenminnomori` は `tel: null`（precheck が番号を控えていない）。唐沢 046-288-1318 ほか5件も未着手 |
| 6 | 千葉の範囲拡張 | 対象8市町の外に候補20件（いすみ4・香取1・成田1・佐倉1・大網白里1＋第4弾12）。**袖ケ浦・市原はアクアライン直後で木更津より近い** |

---

## 3. 数字の現在値

```
data/campgrounds.json   191件（active 147 / unverified 35 / closed 7 / suspended 2）
  └ 千葉 3件（全部 unverified なので /about の147は変わらない）
MUNI_SOURCES            26市区町村（既存18 + 千葉8）
地区 md                 18（案C後） + 76（案C前・再生成されない） + 千葉8
validate                検証OK / 警告18件
モック                   12本すべてパス
デプロイ                 e6849fe6（2026-08-17）。照合語6つとも本番で期待どおり
```

---

## 4. 次のセッションで最初に走らせるもの

```
node scripts/.baseline-before-planc.js      # exit 0 なら土台は無事
npm run validate                            # 検証OK / 警告18
```
